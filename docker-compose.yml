version: '3'
services:
  db:
    image: mysql:8.0
    container_name: db-devbercerita
    ports:
      - "3307:3306"
    environment:
      - MYSQL_ROOT_PASSWORD=jjj
      - MYSQL_DATABASE=devbercerita
    volumes:
      - db_data:/var/lib/mysql

volumes:
  db_data:
